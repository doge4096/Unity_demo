# Unity 肉鸽游戏 — 场景搭建完整指南（中文界面版）

> 适用版本：Unity 2022.3 LTS
> 前置条件：所有 C# 脚本已生成在 `Assets/Scripts/` 下，编译无报错

---

## 第一步：打开项目并等待编译

1. 打开 **Unity Hub** → 点击「打开」→ 选择 `d:\Project\unity\interview`
2. 等底部进度条跑完，**控制台**（Console）没有红色错误即可

---

## 第二步：创建地面和测试靶子

### 2.1 创建地面

- 在 **层级** 窗口空白处右键 → `3D 对象` → `平面`（Plane）
- 命名为 `Ground`
- **检视面板** 中设置：
  - 位置（Position）：`(0, 0, 0)`
  - 缩放（Scale）：`(3, 1, 3)`

### 2.2 创建测试靶子（敌人）

- 层级窗口右键 → `3D 对象` → `立方体`（Cube）
- 命名为 `EnemyTarget`
- 检视面板中设置：
  - 位置：`(3, 0.5, 5)`
  - 缩放：`(1, 1, 1)`
- 点「**添加组件**」→ 搜索 `刚体`（Rigidbody）→ 添加
- 在刚体组件中勾选「**运动学**」（Is Kinematic）

---

## 第三步：创建两个玩家角色

### 3.1 近战角色

- 层级窗口右键 → `3D 对象` → `胶囊体`（Capsule）
- 命名为 `MeleePlayer`
- 检视面板中设置：
  - 位置：`(0, 1, 0)`

添加组件：
1. 点「**添加组件**」→ 搜索 `Character Controller`（角色控制器）→ 添加
   - 中心（Center）：`(0, 1, 0)`
   - 半径（Radius）：`0.4`
   - 高度（Height）：`2`
2. 点「**添加组件**」→ 搜索 `MeleeCharacter` → 添加

创建武器挂点：
- 在 MeleePlayer 上右键 → `创建空对象`
- 命名为 `WeaponSlot`
- 检视面板中设置位置：`(0, 1, 1.5)`
- 点「**添加组件**」→ 搜索 `MeleeWeapon` → 添加

配置武器引用：
- 重新选中 `MeleePlayer` → 在 `MeleeCharacter` 脚本中找到 `Weapon` 字段
- 把 `WeaponSlot` 从层级窗口拖到该字段上

> **最后：取消 MeleePlayer 检视面板左上角的勾选**（让它默认隐藏，选人后才出现）

### 3.2 远程角色

- 选中 MeleePlayer → 按 `Ctrl+D` 复制 → 重命名为 `RangedPlayer`
- 移除 `MeleeCharacter` 组件（点组件右上角 `⋮` → `移除组件`）
- 点「**添加组件**」→ 搜索 `RangedCharacter` → 添加
- **同样取消勾选**让它默认隐藏

---

## 第四步：创建玩家控制器

- 层级窗口右键 → `创建空对象`
- 命名为 `Player`
- 位置：`(0, 0, 0)`
- 点「**添加组件**」→ 搜索 `PlayerController` → 添加

---

## 第五步：创建 GameManager

- 层级窗口右键 → `创建空对象`
- 命名为 `GameManager`
- 点「**添加组件**」→ 搜索 `GameManager` → 添加

在 GameManager 的检视面板中：
- **Current State** 选择 `CharacterSelect`
- **Melee Player Go** → 拖入层级窗口中的 `MeleePlayer`
- **Ranged Player Go** → 拖入层级窗口中的 `RangedPlayer`
- **Player Controller** → 拖入层级窗口中的 `Player`

---

## 第六步：配置 ScriptableObject 数据资产

### 6.1 创建近战角色数据

- **项目** 窗口 → 右键 `Assets/ScriptableObjects/` 文件夹
- `创建` → `Roguelike` → `Character Data`
- 命名为 `MeleeData`
- 检视面板中填写：

| 字段 | 值 |
|------|-----|
| Character Name（角色名称） | `剑士` |
| Character Type（角色类型） | `Melee` |
| Portrait（角色立绘） | 先留空 |
| Description（角色描述） | `近战高防战士，扇形范围攻击，3段连击伤害递增。血量高、防御高，适合正面冲锋。` |
| Max Health（最大生命） | `150` |
| Attack Damage（攻击力） | `20` |
| Attack Range（攻击范围） | `2.5` |
| Attack Cooldown（攻击冷却） | `0.5` |
| Defense（防御） | `10` |
| Move Speed（移动速度） | `5` |

### 6.2 创建远程角色数据

- 同上步骤，右键 → `创建` → `Roguelike` → `Character Data`
- 命名为 `RangedData`

| 字段 | 值 |
|------|-----|
| Character Name | `射手` |
| Character Type | `Ranged` |
| Portrait | 先留空 |
| Description | `远程高伤射手，发射弹道攻击敌人。血量低但攻击高，需要保持距离风筝敌人。` |
| Max Health | `80` |
| Attack Damage | `25` |
| Attack Range | `15` |
| Attack Cooldown | `0.8` |
| Defense | `3` |
| Move Speed | `6` |

### 6.3 创建 Buff 数据

- 右键 `Assets/ScriptableObjects/` → `创建` → `Roguelike` → `Buff Data`
- 创建 4 个：

| 文件名 | Buff Name | Type | Value |
|--------|-----------|------|-------|
| `Buff_AttackUp` | 攻击力提升 | AttackUp | 10 |
| `Buff_DefenseUp` | 防御力提升 | DefenseUp | 5 |
| `Buff_SpeedUp` | 移速提升 | SpeedUp | 1.5 |
| `Buff_MaxHPUp` | 生命上限提升 | MaxHPUp | 30 |

### 6.4 把 Buff 拖入 GameManager

- 选中层级中的 `GameManager`
- 在检视面板中找到底部的 `RewardSelector` 组件
- 找到 **Buff Pool** 字段 → 点 `+` 号 4 次
- 把上面 4 个 Buff 资产从项目窗口一一拖进去

---

## 第七步：搭建选人界面 UI

### 7.1 创建画布

- 层级窗口右键 → `UI` → `画布`（Canvas）
- 选中 Canvas，检视面板中：
  - **渲染模式**（Render Mode）改为 `Screen Space - Camera`
  - **渲染摄像机**（Render Camera）拖入层级中的 `Main Camera`

### 7.2 创建选人面板

- 在 Canvas 上右键 → `UI` → `面板`（Panel）
- 命名为 `CharacterSelectPanel`
- 全屏拉伸锚点：点击 Rect Transform 左上角锚点按钮 → 按住 `Alt` → 选右下角拉伸 → Left/Right/Top/Bottom 全设为 `0`

### 7.3 创建标题

- CharacterSelectPanel 上右键 → `UI` → `Text - TextMeshPro`
- 命名为 `Title`
- 文字：`选择你的角色`，字号：`36`，居中，位置 Y：`200`

### 7.4 创建近战角色卡片

- CharacterSelectPanel 上右键 → `UI` → `面板`（Panel）
- 命名为 `MeleeCard`，宽 `300`，高 `450`，位置 X：`-250`
- 在 MeleeCard 下创建：

| 元素 | 类型 | 命名 | 说明 |
|------|------|------|------|
| 立绘 | 图像（Image） | `MeleePortrait` | 宽200 高200 位置Y:100 |
| 名称 | Text - TextMeshPro | `MeleeName` | 位置Y:-20 |
| 描述 | Text - TextMeshPro | `MeleeDescription` | 宽260 位置Y:-80 字号12 |
| 属性 | Text - TextMeshPro | `MeleeStats` | 宽260 位置Y:-170 字号12 |
| 按钮 | 按钮（Button） | `SelectMeleeButton` | 文字：`选择剑士` 宽200 位置Y:-200 |

### 7.5 创建远程角色卡片

- 选中 MeleeCard → `Ctrl+D` → 改名 `RangedCard`
- 位置 X 改为 `250`
- 子元素逐一改名（Melee→Ranged），按钮文字改 `选择射手`

### 7.6 挂载 CharacterSelectPanel 脚本

- 选 `CharacterSelectPanel` → 添加组件 → `CharacterSelectPanel`
- 拖入所有引用：

| 脚本字段 | 拖入内容 |
|----------|----------|
| Melee Data | 项目窗口 `MeleeData` 资产 |
| Ranged Data | 项目窗口 `RangedData` 资产 |
| 所有 MeleeCard 子元素 | 层级窗口对应 UI 组件 |
| 所有 RangedCard 子元素 | 层级窗口对应 UI 组件 |
| Panel Root | CharacterSelectPanel 自身 |

---

## 第八步：搭建游戏 HUD

### 8.1 创建 HUD 画布

- 层级窗口右键 → `UI` → `画布`（Canvas）
- 命名为 `HUDCanvas`
- 渲染模式保持 `Screen Space - Overlay`

### 8.2 血条

- HUDCanvas 上右键 → `UI` → `滑动条`（Slider）
- 命名为 `HealthBar`，宽 `300`
- 删除子节点中的 `Handle Slide Area`

### 8.3 血量文本

- HealthBar 上右键 → `UI` → `Text - TextMeshPro`
- 命名为 `HealthText`

### 8.4 角色信息

- HUDCanvas 上右键 → `UI` → `图像` → `CharacterPortrait`（50×50）
- HUDCanvas 上右键 → `UI` → `Text` → `CharacterNameText`

### 8.5 房间进度

- HUDCanvas 上右键 → `UI` → `Text` → `RoomProgress`
- 文字：`房间 1 / 10`，放右上角

### 8.6 Buff 列表

- HUDCanvas 上右键 → `创建空对象` → `BuffList`

### 8.7 挂载 HUD 脚本

- 选 `HUDCanvas` → 添加组件 → `HUD`
- 拖入所有引用

---

## 第九步：创建结算界面

### 9.1 GameOver 面板

- HUDCanvas 上右键 → `UI` → `面板` → `GameOverPanel`
- 全屏拉伸，半透明黑底
- **取消勾选** 默认隐藏

子元素：

| 元素 | 类型 | 命名 | 说明 |
|------|------|------|------|
| 标题 | Text | `ResultTitle` | 字号48 居中 位置Y:100 |
| 统计×3 | Text | `RoomsCleared` / `BuffsCollected` / `Difficulty` | 位置Y:0/-50/-100 |
| 按钮×2 | Button | `RestartButton` / `MenuButton` | 文字：再来一局 / 返回主菜单 |

### 9.2 挂载 GameOverPanel 脚本

- 选 `GameOverPanel` → 添加组件 → `GameOverPanel`
- 拖入所有子元素引用

---

## 第十步：设置相机

- 选中 `Main Camera`
- 位置：`(0, 5, -8)`，旋转：`(30, 0, 0)`
- 添加组件 → `ThirdPersonCamera`

---

## 第十一步：创建弹道预制体

- 层级右键 → `3D 对象` → `球体`（Sphere）
- 命名 `ProjectilePrefab`，缩放 `(0.3, 0.3, 0.3)`
- 添加组件 → `刚体`（Rigidbody）+ `Projectile`
- 拖入 `Assets/Prefabs/` 文件夹变成预制体
- 删除场景中的球体
- 选 `RangedPlayer` → `Projectile Prefab` 字段拖入预制体

---

## 第十二步：最终验证

按 ▶️ 播放按钮，检查：

| 检查项 | 预期 |
|--------|------|
| 选人界面 | 两张角色卡片，显示名称、描述、属性 |
| 选近战角色 | 面板消失，胶囊体出现，WASD 移动 |
| 鼠标右键 | 旋转视角 |
| 鼠标左键 | 控制台输出攻击日志 |
| 选远程角色 | 左键发射球体弹道 |
| 控制台 | 无红色报错 |
